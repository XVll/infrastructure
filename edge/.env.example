# Cloudflare API (for Let's Encrypt DNS challenge)
CF_API_EMAIL=op://Server/cloudflare/email
CF_DNS_API_TOKEN=op://Server/cloudflare/api_token

# Redis Connection (when using shared Redis from data VM)
REDIS_PASSWORD=op://Server/redis/password

# Authentik PostgreSQL Connection (when using shared PostgreSQL from data VM)
AUTHENTIK_DB_PASSWORD=op://Server/authentik/db_password

# Authentik Secret Key
AUTHENTIK_SECRET_KEY=op://Server/authentik/secret_key
