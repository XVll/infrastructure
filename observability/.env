# OBSERVABILITY HOST ENVIRONMENT VARIABLES
# This file is committed to git (safe - contains only op:// references, not actual passwords)
# Deploy: op run --env-file=.env -- docker compose up -d

# ============================================================================
# 1PASSWORD STRUCTURE
# ============================================================================
# Vault: "Server"
# Items you need to create in 1Password:
#   - mongodb (fields: username, password) - Used by Komodo and Graylog
#   - grafana (fields: username, password) - for Grafana admin
#   - graylog (fields: password-secret, root-password-sha2)

# ============================================================================
# MONGODB - Shared with db host (10.10.10.111:27017)
# ============================================================================
# Used by Komodo and Graylog
MONGODB_ROOT_USER=op://Server/mongodb/username
MONGODB_ROOT_PASSWORD=op://Server/mongodb/password

# ============================================================================
# GRAFANA
# ============================================================================
GRAFANA_ADMIN_USER=op://Server/grafana/username
GRAFANA_ADMIN_PASSWORD=op://Server/grafana/password

# ============================================================================
# GRAYLOG
# ============================================================================
# Graylog secrets
# password-secret: Random string (minimum 16 characters) - generate with: pwgen -N 1 -s 96
# root-password-sha2: SHA-256 hash of your admin password - generate with: echo -n "yourpassword" | sha256sum
GRAYLOG_PASSWORD_SECRET=op://Server/graylog/password-secret
GRAYLOG_ROOT_PASSWORD_SHA2=op://Server/graylog/root-password-sha2
